spring:
  data:
    mongodb:
      database: test
  shell:
    interactive:
      enabled: true
    noninteractive:
      enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5

mongock:
  enabled: false